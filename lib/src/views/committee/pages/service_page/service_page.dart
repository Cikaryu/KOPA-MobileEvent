import 'package:flutter/material.dart';

class ServiceCommitteePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Service Page', style: TextStyle(fontSize: 35, fontWeight: FontWeight.bold)),
    );
  }
}
