import 'package:flutter/material.dart';

class EventCommitteePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Event Page', style: TextStyle(fontSize: 35, fontWeight: FontWeight.bold)),
    );
  }
}
