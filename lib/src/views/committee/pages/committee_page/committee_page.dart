import 'package:flutter/material.dart';

class CommitteePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Participant Page', style: TextStyle(fontSize: 35, fontWeight: FontWeight.bold)),
    );
  }
}
